{
  "scripts": {
    "docs:build": "npm run build -w venoui.com",
    "docs:publish": "NODE_DEBUG=gh-pages gh-pages -d packages/docs/dist",
    "docs:update": "npm run docs:build && npm run docs:publish"
  },
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": ">=12"
  },
  "devDependencies": {
    "gh-pages": "^3.2.3",
    "sass": "^1.39.0"
  }
}
