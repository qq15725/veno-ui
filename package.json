{
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "docs:build": "npm run build -w venoui.com",
    "docs:publish": "NODE_DEBUG=gh-pages gh-pages -d packages/docs/dist"
  },
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": ">=12"
  },
  "devDependencies": {
    "gh-pages": "^3.2.3",
    "sass": "^1.39.0"
  }
}
